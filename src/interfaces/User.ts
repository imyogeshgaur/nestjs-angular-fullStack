interface User {
    firstName: String,
    lastName: String,
    emailOfUser: String,
    phoneNumber: Number,
    password: String
}

interface loginUser {
    emailOfUser: String,
    password: String;
}
